extends: [base_package]

build_stages:
- name: configure
  after: prologue
  handler: bash
  bash: | 
    ./configure FC={{fortranCompiler}}
     
- name: make
  after: configure
  handler: bash
  bash: |
    make 

